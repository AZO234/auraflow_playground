# comfyui_playground 直接依存のみ。ComfyUI 本体・Impact-Pack・Impact-Subpack の依存は
# それぞれの requirements.txt を別途インストールする方針 (重複させない、README 参照)。
#
# PyTorch は環境に合わせて個別にインストールする (この requirements には含めない)
#   CPU 版 :  pip install --index-url https://download.pytorch.org/whl/cpu  torch torchvision
#   CUDA12 :  pip install --index-url https://download.pytorch.org/whl/cu124 torch torchvision
#   CUDA13 :  pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
# 参照: https://pytorch.org/get-started/locally/

# safetensors I/O (common.save_file)
safetensors

# 画像 (common / pngutil / gallery / generate_gui)
Pillow

# 設定ファイル (Python 3.11+ は tomllib 標準。3.10 互換用に tomli/tomli-w)
tomli
tomli-w

# ADetailer YOLO モデル + Real-ESRGAN アップスケールモデル等の自動 DL (generate.py)
# (AuraFlow は all-in-one に VAE 同梱なので VAE 単体の自動 DL は不要)
huggingface-hub

# ComfyUI HTTP API への multipart POST 画像アップロード (generate.py)
# + Gemma 翻訳/整形の Ollama HTTP 呼び出し (--translate, common.refine_prompt_text)
requests

# --translate (日本語→英語プロンプト翻訳 / 合成英語の自然文化) は Ollama を外部サービスとして
# 利用する (pip 依存ではない)。別途インストールして Gemma を pull しておく:
#   Ollama 本体 : https://ollama.com/download
#   モデル取得  : ollama pull gemma3:4b   (8GB VRAM 向け既定。--ollama-model で変更可)
# Ollama 未起動・モデル未取得でも生成は動く (翻訳をスキップして原文プロンプトを使用)。

# ComfyUI HTTP API への WebSocket 進捗ストリーミング (generate.py)
websocket-client

# ComfyUI プロセスの kill+restart (generate.py --arch cpu/cuda 切替時)
psutil

# 対話 TUI (lora_chance_ui.py の矢印+Space 多選択)
questionary

# generate_gui.py のリファレンス画像ドラッグ&ドロップ (Tkinter に D&D 標準が無いので追加)
# 無くても GUI 起動はする (D&D だけ no-op になる)
tkinterdnd2
